<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Paclink.UI.MAUI.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="CenterAndExpand">

            <!-- equivalent to left hand side of Windows UI -->
            <Border>
                <Editor IsReadOnly="True" />
            </Border>

            <!-- equivalent to right hand side of Windows UI -->
            <Border>
                <Editor IsReadOnly="True" />
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

