namespace Paclink
{
    public enum ConnectionOrigin
    {
        // Enumeration to indicate the origin of a connection...
        Disconnected,
        OutboundConnection,
        InboundConnection
    }
}
